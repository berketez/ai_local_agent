# Core
rich>=13.0.0
requests>=2.28.0
pyautogui>=0.9.53
pillow>=9.0.0
pytesseract>=0.3.10

# LLM Backends
ollama>=0.1.0

# Browser Automation — Playwright + CDP (tercih edilen, openclaw'dan port)
# Kurulum: pip install playwright && playwright install chromium
# (veya mevcut Chrome'a CDP ile bağlan — indirme gerekmez)
playwright>=1.48.0

# Browser Automation — Selenium (Playwright yoksa fallback)
selenium>=4.0.0
webdriver-manager>=4.0.0

# Optional: LM Studio OpenAI-compatible API
openai>=1.0.0
